/**
 * Usando o destructuring, crie uma função que recebe o objeto
 * abaixo como parâmetro e retorna uma string com os valores
 * das chaves concatenados numa string. Ex.:
 * "data title with options: a, b and c"
 */

const data = {
  title: "data title",
  details: {
    id: 1,
    options: ["a", "b", "c"],
  },
};
